// File: web/postcss.config.mjs
// Tailwind v4 PostCSS entry (CSS-first config lives in app/globals.css @theme).
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
