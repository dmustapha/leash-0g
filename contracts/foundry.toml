[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.30"
evm_version = "cancun"
optimizer = true
optimizer_runs = 200

[fuzz]
runs = 512

[invariant]
runs = 128
depth = 64

[rpc_endpoints]
zg_testnet = "${ZERO_G_RPC}"
